---
- name: Converge
  hosts: all
  become: true
  vars:
    molecule_is_test: true
    cert_manager_email: loic.roux.404@k3s.local
    kubeapps_dex_github_client_id: "dedb54ac672994b1b139"
    kubeapps_dex_github_client_secret: "89baa560ca17e4fe1e89deb4292c458ec7456449"
    kubeapps_dex_github_client_org: "esgi-lyon"
    kubeapps_dex_github_client_team: "ops-team"

  roles:
    - role: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | basename }}"

  pre_tasks:
    - name: Ensure test dependencies are installed.
      package: 
        name:
          - iptables
          - curl
        state: present
        update_cache: true
      when: ansible_os_family == 'Debian'
